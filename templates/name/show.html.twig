{% extends 'base.html.twig' %}

{% block title %}Name{% endblock %}

{% block body %}
    <h1>Name</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ name.id }}</td>
            </tr>
            <tr>
                <th>Fullname</th>
                <td>{{ name.fullname }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ name.birthday ? name.birthday|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ name.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_name_index') }}">back to list</a>

    <a href="{{ path('app_name_edit', {'id': name.id}) }}">edit</a>

    {{ include('name/_delete_form.html.twig') }}
{% endblock %}
