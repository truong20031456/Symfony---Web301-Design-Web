{% extends 'base.html.twig' %}

{% block title %}Edit Name{% endblock %}

{% block body %}
    <h1>Edit Name</h1>

    {{ include('name/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_name_index') }}">back to list</a>

    {{ include('name/_delete_form.html.twig') }}
{% endblock %}
